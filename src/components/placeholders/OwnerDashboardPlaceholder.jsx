import RoutePlaceholder from "./RoutePlaceholder";

// Route /chatelain/dashboard — tableau de bord châtelain.
// Vraie implémentation : Sprint S2-γ.
export default function OwnerDashboardPlaceholder() {
  return <RoutePlaceholder route="/chatelain/dashboard" />;
}
