import RoutePlaceholder from "./RoutePlaceholder";

// Route /admin/dashboard — tableau de bord admin LCC.
// Vraie implémentation : Sprint S2-ε.
export default function AdminDashboardPlaceholder() {
  return <RoutePlaceholder route="/admin/dashboard" />;
}
