import RoutePlaceholder from "./RoutePlaceholder";

// Route /auth/callback — handler du lien magique Supabase (décision #4).
// Vraie implémentation : Sprint S2-α.2 (récupère la session, redirige).
export default function AuthCallbackPlaceholder() {
  return <RoutePlaceholder route="/auth/callback" />;
}
