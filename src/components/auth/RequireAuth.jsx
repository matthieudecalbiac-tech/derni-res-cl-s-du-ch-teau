// TODO: Stub α.1 — remplacer par un check de session Supabase en α.2
// (redirige vers /auth/callback ou affiche <AuthForm> si pas de session).
// Pour l'instant : laisse passer (l'auth n'existe pas encore).
export default function RequireAuth({ children }) {
  return children;
}
