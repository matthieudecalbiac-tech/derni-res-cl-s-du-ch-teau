// TODO: Stub α.1 — vérifier user.role (client / chatelain / admin) en α.2/α.3.
// Pour l'instant : laisse passer quel que soit le `role` demandé.
export default function RequireRole({ children /* , role */ }) {
  return children;
}
